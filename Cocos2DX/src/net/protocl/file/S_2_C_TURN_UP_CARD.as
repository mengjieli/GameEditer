package net.protocl.file{	import net.protocl.MyByteArray;	import net.protocl.MessageBase;	import net.protocl.IMessage;	public class S_2_C_TURN_UP_CARD extends MessageBase implements IMessage	{		public var index:int = 0;		public var exp:int = 0;		public var money:int = 0;		public var itemId:int = 0;		public var count:int = 0;		public function S_2_C_TURN_UP_CARD()		{			super(ProtoclId.S_2_C_TURN_UP_CARD);		}		//打包函数		public function encode():MyByteArray		{			var bytes:MyByteArray = new MyByteArray();			encodeShort(bytes,index);			encodeInt32(bytes,exp);			encodeInt32(bytes,money);			encodeInt32(bytes,itemId);			encodeShort(bytes,count);			return bytes;		}		//解包函数		public function decode(bytes:MyByteArray):void		{			index = decodeShort(bytes);			exp = decodeInt32(bytes);			money = decodeInt32(bytes);			itemId = decodeInt32(bytes);			count = decodeShort(bytes);		}		//打印函数		override public function getString(func,p:String="")		{			func(p + "{");			p += "	";			func(p + "index:" + index);			func(p + "exp:" + exp);			func(p + "money:" + money);			func(p + "itemId:" + itemId);			func(p + "count:" + count);			p = p.slice(1,p.length);			func(p + "}");		}	}}
package net.protocl.file{	import net.protocl.MyByteArray;	import net.protocl.MessageBase;	import net.protocl.IMessage;	public class S2C_LoadRoles extends MessageBase implements IMessage	{		public var rolers:Vector.<S2C_LoadRolesInfo> = new Vector.<S2C_LoadRolesInfo>;		public function S2C_LoadRoles()		{			super(ProtoclId.S2C_LoadRoles);		}		//打包函数		public function encode():MyByteArray		{			var bytes:MyByteArray = new MyByteArray();			encodeRepeatMessage(bytes,rolers);			return bytes;		}		//解包函数		public function decode(bytes:MyByteArray):void		{			decodeRepeatMessage(bytes,S2C_LoadRolesInfo,rolers);		}		//打印函数		override public function getString(func,p:String="")		{			func(p + "{");			p += "	";			func(p + "[");			for(var i:int = 0; i < rolers.length; i++)			{				this.rolers[i].getString(func,p + "	");			}			func(p + "]");			p = p.slice(1,p.length);			func(p + "}");		}	}}